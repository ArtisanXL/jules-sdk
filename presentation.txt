🏃‍♂️ Pacer: Implemented CLI support and subcommands

💡 What: Implemented the `jules-cli` crate by setting up argument parsing using `clap`. Created core subcommands (`chat` and `config`). Implemented configuration file loading from disk using the `dirs` and `serde` crates, with CLI flag overrides. Also added the ability to output results in Plain or JSON formats, and wrote CLI smoke tests. Updates to PROJECT_STATE.md reflect the completion of all 5 subtasks under PH3-03.

🎯 Why: To complete Order 10 (PH3-03) of the project state, advancing the tooling & CLI capabilities of the Jules SDK. This gives users a functioning command-line utility to test and interact with the project features easily.

📊 Impact: Completes Phase 3 subtasks `PH3-03.1` through `PH3-03.5`. Modifies project metrics (Subtasks done 44/33 remaining, Overall progress 57%).

🔬 Measurement: `cargo test -p jules-cli` and `cargo test --workspace` run successfully, along with `cargo clippy --workspace --all-features -- -D warnings`.
